import express from 'express';import {listSuppliers,createSupplier,listRequests,createRequest,listQuotes,createQuote,listOrders,createOrder,approveOrder,listReceipts,createReceipt,listSupplierInvoices,createSupplierInvoice} from '../controllers/procurementController.js';const r=express.Router();r.get('/suppliers',listSuppliers);r.post('/suppliers',createSupplier);r.get('/requests',listRequests);r.post('/requests',createRequest);r.get('/quotes',listQuotes);r.post('/quotes',createQuote);r.get('/orders',listOrders);r.post('/orders',createOrder);r.patch('/orders/:id/approve',approveOrder);r.get('/receipts',listReceipts);r.post('/receipts',createReceipt);r.get('/invoices',listSupplierInvoices);r.post('/invoices',createSupplierInvoice);export default r;
