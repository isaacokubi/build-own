import express from 'express';import {listIncidents,createIncident,listInspections,createInspection,listQuality,createQuality,listActions,createAction,closeAction} from '../controllers/safetyQualityController.js';const r=express.Router();r.get('/incidents',listIncidents);r.post('/incidents',createIncident);r.get('/inspections',listInspections);r.post('/inspections',createInspection);r.get('/quality',listQuality);r.post('/quality',createQuality);r.get('/actions',listActions);r.post('/actions',createAction);r.patch('/actions/:id/complete',closeAction);export default r;
