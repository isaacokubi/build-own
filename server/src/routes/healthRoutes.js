import express from 'express';import mongoose from 'mongoose';const r=express.Router();r.get('/live',(_req,res)=>res.json({success:true,status:'alive',timestamp:new Date().toISOString()}));r.get('/ready',(_req,res)=>{const ready=mongoose.connection.readyState===1;res.status(ready?200:503).json({success:ready,status:ready?'ready':'not_ready',database:ready?'connected':'disconnected',timestamp:new Date().toISOString()})});export default r;
