import express from 'express';import {employees,attendance,leaves,timesheets,payrolls,payslips,createEmployee,createAttendance,createLeave,createTimesheet,createPayroll,approvePayroll} from '../controllers/hrPayrollController.js';const r=express.Router();r.get('/employees',employees);r.post('/employees',createEmployee);r.get('/attendance',attendance);r.post('/attendance',createAttendance);r.get('/leave',leaves);r.post('/leave',createLeave);r.get('/timesheets',timesheets);r.post('/timesheets',createTimesheet);r.get('/payroll',payrolls);r.post('/payroll',createPayroll);r.patch('/payroll/:id/approve',approvePayroll);r.get('/payslips',payslips);export default r;
