export function observability(req,res,next){const started=process.hrtime.bigint();res.on('finish',()=>{const ms=Number(process.hrtime.bigint()-started)/1e6;console.info(JSON.stringify({event:'http_request',requestId:req.requestId,method:req.method,path:req.originalUrl,status:res.statusCode,durationMs:Number(ms.toFixed(2)),tenantId:req.user?.tenantId||null,userId:req.user?.id||req.user?._id||null}))});next()}
