import mongoose from 'mongoose';
const clientSchema=new mongoose.Schema({tenantId:{type:mongoose.Schema.Types.ObjectId,ref:'Tenant',required:true,index:true},name:{type:String,required:true,trim:true},type:{type:String,enum:['individual','company'],default:'company'},email:{type:String,trim:true,lowercase:true},phone:String,address:String,registrationNumber:String,notes:String,isDeleted:{type:Boolean,default:false,index:true}},{timestamps:true});clientSchema.index({tenantId:1,name:1});clientSchema.index({tenantId:1,email:1});export default mongoose.model('Client',clientSchema);
