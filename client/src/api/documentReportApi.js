import axios from 'axios';const api=axios.create({baseURL:`${import.meta.env.VITE_API_URL||'http://localhost:5000/api'}/documents-reporting`,withCredentials:true});api.interceptors.request.use(c=>{const t=localStorage.getItem('token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});export const getDocuments=()=>api.get('/documents');export const createDocument=d=>api.post('/documents',d);export const getReports=()=>api.get('/reports');export const createReport=d=>api.post('/reports',d);
