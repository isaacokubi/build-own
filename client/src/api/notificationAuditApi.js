import axios from 'axios';const api=axios.create({baseURL:`${import.meta.env.VITE_API_URL||'http://localhost:5000/api'}/notifications-audit`,withCredentials:true});api.interceptors.request.use(c=>{const t=localStorage.getItem('token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});export const getNotifications=()=>api.get('/notifications');export const markNotificationRead=id=>api.patch(`/notifications/${id}/read`);export const createNotification=d=>api.post('/notifications',d);export const getAuditLogs=()=>api.get('/audit-logs');export const createAuditLog=d=>api.post('/audit-logs',d);
