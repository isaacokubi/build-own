import axios from 'axios';const api=axios.create({baseURL:`${import.meta.env.VITE_API_URL||'http://localhost:5000/api'}/inventory`,withCredentials:true});api.interceptors.request.use(c=>{const t=localStorage.getItem('token');if(t)c.headers.Authorization=`Bearer ${t}`;return c});export const getMaterials=()=>api.get('/materials');export const createMaterial=d=>api.post('/materials',d);export const getWarehouses=()=>api.get('/warehouses');export const createWarehouse=d=>api.post('/warehouses',d);export const getStock=()=>api.get('/stock');export const getInventorySummary=()=>api.get('/summary');export const recordStockMovement=d=>api.post('/movements',d);
