import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {tenantApi} from '../api';
export default function TenantAdmin(){const[tenant,setTenant]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{tenantApi.me().then(r=>setTenant(r.data?.data||r.data)).catch(e=>setError(e.response?.data?.message||'Unable to load company settings')).finally(()=>setLoading(false))},[]);if(loading)return <main className="shell"><div className="card">Loading company workspace...</div></main>;return <main className="shell"><header><div><p className="eyebrow">COMPANY ADMIN</p><h1>{tenant?.name||'Company Workspace'}</h1><p className="muted">Manage your company's workspace without accessing another tenant's data.</p></div><Link className="button secondary" to="/operations">Operations</Link></header>{error&&<div className="alert">{error}</div>}<div className="hubGrid"><Link className="hubCard" to="/construction"><h2>Projects</h2><p>Manage projects, clients, contracts, BOQs and budgets.</p><span>Open projects →</span></Link><Link className="hubCard" to="/finance"><h2>Finance</h2><p>Review expenses, revenue, procurement and financial operations.</p><span>Open finance →</span></Link><Link className="hubCard" to="/hr"><h2>Workforce</h2><p>Manage employees, attendance and workforce administration.</p><span>Open HR →</span></Link><Link className="hubCard" to="/documents-reporting"><h2>Documents & Reports</h2><p>Access company documents and reporting data.</p><span>Open documents →</span></Link></div></main>}
