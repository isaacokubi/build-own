import {useEffect,useState} from 'react';import {getDocuments,getReports} from '../api/documentReportApi';export default function DocumentsReporting(){const[d,setD]=useState({documents:[],reports:[]}),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{Promise.all([getDocuments(),getReports()]).then(([a,b])=>setD({documents:a.data?.data||[],reports:b.data?.data||[]})).catch(e=>setError(e.response?.data?.message||'Unable to load documents and reports')).finally(()=>setLoading(false))},[]);if(loading)return <div className="p-6">Loading documents & reports...</div>;return <div className="p-6 space-y-6"><div><h1 className="text-2xl font-bold">Documents & Reports</h1><p className="text-sm opacity-70">Centralized tenant documents and generated business reports.</p></div>{error&&<div className="rounded border p-3">{error}</div>}<div className="grid gap-4 md:grid-cols-2"><div className="rounded-xl border p-5"><div className="text-sm opacity-70">Documents</div><div className="mt-2 text-3xl font-bold">{d.documents.length}</div></div><div className="rounded-xl border p-5"><div className="text-sm opacity-70">Reports</div><div className="mt-2 text-3xl font-bold">{d.reports.length}</div></div></div><div className="overflow-x-auto rounded-xl border"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">Document</th><th className="p-3">Category</th><th className="p-3">Visibility</th><th className="p-3">Created</th></tr></thead><tbody>{d.documents.map(x=><tr className="border-b" key={x._id}><td className="p-3 font-medium">{x.name}</td><td className="p-3">{x.category}</td><td className="p-3">{x.visibility}</td><td className="p-3">{new Date(x.createdAt).toLocaleDateString()}</td></tr>)}{!d.documents.length&&<tr><td colSpan="4" className="p-6 text-center">No documents available.</td></tr>}</tbody></table></div></div>}
