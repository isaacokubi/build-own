import {BrowserRouter,Link,Route,Routes} from 'react-router-dom';import {tenantApi} from './api';import {useEffect,useState} from 'react';import ConstructionCore from './ConstructionCore';import FinanceProcurement from './FinanceProcurement';import HRWorkforce from './HRWorkforce';import Payroll from './Payroll';import OperationsHub from './pages/OperationsHub';import SafetyQualityManagement from './pages/SafetyQualityManagement';import DocumentsReporting from './pages/DocumentsReporting';import TenantAdmin from './pages/TenantAdmin';import EquipmentInventory from './EquipmentInventory';import './index.css';

const modules=[['Projects','Projects, contracts, BOQs and budgets','/construction'],['Finance','Budgets, procurement and approvals','/finance'],['Workforce','HR, attendance and workforce','/hr'],['Payroll','Payroll runs and compensation','/payroll'],['Safety & Quality','Incidents, inspections and corrective actions','/safety-quality'],['Documents','Documents, reports and records','/documents-reporting'],['Equipment','Equipment and inventory control','/equipment-inventory'],['Administration','Tenant administration and settings','/admin']];

function Dashboard(){return <main className="shell"><header><div><p className="eyebrow">BUILD-OWN PLATFORM</p><h1>Construction Management System</h1><p className="muted">A secure multi-tenant workspace for running modern construction operations.</p></div><div className="actions"><Link className="button" to="/operations">Open operations hub</Link><Link className="button secondary" to="/superadmin/companies">Manage companies</Link></div></header><section className="hero"><h2>Construction Control Center</h2><p>Coordinate projects, contracts, BOQs, budgets, procurement, workforce, safety, quality, documents and reporting from one operational workspace while keeping every company's data isolated.</p></section><div className="sectionTitle"><div><h2>Workspace modules</h2><p>Jump directly into the area you need to manage.</p></div></div><section className="hubGrid">{modules.map(([title,desc,to])=><Link className="hubCard" to={to} key={to}><h2>{title}</h2><p>{desc}</p><span>Open module →</span></Link>)}</section></main>}

function Companies(){const[tenants,setTenants]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[search,setSearch]=useState(''),[busy,setBusy]=useState(null);const load=async()=>{setLoading(true);setError('');try{const r=await tenantApi.list({search:search||undefined});setTenants(r.data.data||[])}catch(e){setError(e.response?.data?.message||'Unable to load companies.')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const status=async(id,value)=>{setBusy(id);try{await tenantApi.setStatus(id,value);await load()}catch(e){setError(e.response?.data?.message||'Unable to update company.')}finally{setBusy(null)}};const remove=async t=>{if(t.isSystem||window.prompt(`Permanently delete ${t.name}? Type DELETE to confirm.`)!=='DELETE')return;setBusy(t._id);try{await tenantApi.remove(t._id);await load()}catch(e){setError(e.response?.data?.message||'Unable to delete company.')}finally{setBusy(null)}};return <main className="shell"><header><div><p className="eyebrow">SUPERADMIN</p><h1>Companies</h1><p className="muted">Manage tenant organizations, status and subscriptions.</p></div><div className="actions"><Link className="button secondary" to="/">Dashboard</Link><Link className="button" to="/operations">Operations</Link></div></header><div className="moduleGrid"><div className="metric"><span className="metric-label">Total companies</span><strong className="metric-value">{tenants.length}</strong></div><div className="metric"><span className="metric-label">Active</span><strong className="metric-value">{tenants.filter(t=>t.status==='active').length}</strong></div><div className="metric"><span className="metric-label">Suspended</span><strong className="metric-value">{tenants.filter(t=>t.status==='suspended').length}</strong></div><div className="metric"><span className="metric-label">System tenants</span><strong className="metric-value">{tenants.filter(t=>t.isSystem).length}</strong></div></div><section className="toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search by company name, slug or contact..."/><button onClick={load}>Search</button></section>{error&&<div className="alert">{error}</div>}{loading?<div className="card">Loading companies...</div>:!tenants.length?<div className="card empty">No companies found.</div>:<div className="tableWrap"><table><thead><tr><th>Company</th><th>Owner</th><th>Contact</th><th>Status</th><th>Subscription</th><th>Management</th></tr></thead><tbody>{tenants.map(t=><tr key={t._id}><td><strong>{t.name}</strong><span className="sub">{t.slug}</span></td><td>{t.owner?.name||'—'}<span className="sub">{t.owner?.email||'—'}</span></td><td>{t.email||'—'}<span className="sub">{t.phone||'—'}</span></td><td><span className={`badge ${t.status}`}>{t.status}</span></td><td>{t.subscription?.plan||'starter'}</td><td className="actions"><button disabled={busy===t._id} onClick={()=>status(t._id,t.status==='active'?'suspended':'active')}>{t.status==='active'?'Suspend':'Activate'}</button><button disabled={t.isSystem||busy===t._id} className="danger" onClick={()=>remove(t)}>Delete</button></td></tr>)}</tbody></table></div>}</main>}

export default function App(){return <BrowserRouter><Routes><Route path="/" element={<Dashboard/>}/><Route path="/operations" element={<OperationsHub/>}/><Route path="/admin" element={<TenantAdmin/>}/><Route path="/construction" element={<ConstructionCore/>}/><Route path="/finance" element={<FinanceProcurement/>}/><Route path="/hr" element={<HRWorkforce/>}/><Route path="/payroll" element={<Payroll/>}/><Route path="/equipment-inventory" element={<EquipmentInventory/>}/><Route path="/safety-quality" element={<SafetyQualityManagement/>}/><Route path="/documents-reporting" element={<DocumentsReporting/>}/><Route path="/superadmin/companies" element={<Companies/>}/></Routes></BrowserRouter>}
