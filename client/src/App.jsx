import React,{useEffect,useState} from 'react';import {BrowserRouter,Link,Route,Routes} from 'react-router-dom';import {tenantApi} from './api';import ConstructionCore from './ConstructionCore';import FinanceProcurement from './FinanceProcurement';import './index.css';
function Dashboard(){return <main className="shell"><header><div><p className="eyebrow">BUILD-OWN PLATFORM</p><h1>Construction Management System</h1><p className="muted">Multi-tenant construction operations platform.</p></div><div className="actions"><Link className="button" to="/construction">Construction operations</Link><Link className="button" to="/finance">Finance & procurement</Link><Link className="button secondary" to="/superadmin/companies">Manage companies</Link></div></header><section className="hero"><h2>Construction Control Center</h2><p>Manage projects, clients, contracts, BOQs, finance and procurement while keeping every company's data isolated.</p></section></main>}
function Companies(){const[tenants,setTenants]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[search,setSearch]=useState(''),[busy,setBusy]=useState(null);const load=async()=>{setLoading(true);try{const r=await tenantApi.list({search:search||undefined});setTenants(r.data.data||[])}catch(e){setError(e.response?.data?.message||'Unable to load companies.')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const status=async(id,value)=>{setBusy(id);try{await tenantApi.setStatus(id,value);await load()}catch(e){setError(e.response?.data?.message||'Unable to update company.')}finally{setBusy(null)}};const remove=async t=>{if(t.isSystem||window.prompt(`Permanently delete ${t.name}? Type DELETE to confirm.`)!=='DELETE')return;setBusy(t._id);try{await tenantApi.remove(t._id);await load()}catch(e){setError(e.response?.data?.message||'Unable to delete company.')}finally{setBusy(null)}};return <main className="shell"><header><div><p className="eyebrow">SUPERADMIN</p><h1>Companies</h1><p className="muted">Manage every construction company on the platform.</p></div><Link className="button secondary" to="/">Dashboard</Link></header><section className="toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search companies..."/><button className="button" onClick={load}>Search</button></section>{error&&<div className="alert">{error}</div>}{loading?<div className="card">Loading companies...</div>:!tenants.length?<div className="card empty">No companies found.</div>:<div className="tableWrap"><table><thead><tr><th>Company</th><th>Owner</th><th>Contact</th><th>Status</th><th>Subscription</th><th>Management</th></tr></thead><tbody>{tenants.map(t=><tr key={t._id}><td><strong>{t.name}</strong><span className="sub">{t.slug}</span></td><td>{t.owner?.name||'—'}<span className="sub">{t.owner?.email||'—'}</span></td><td>{t.email||'—'}<span className="sub">{t.phone||'—'}</span></td><td><span className="badge">{t.status}</span></td><td>{t.subscription?.plan||'starter'}</td><td className="actions"><button disabled={busy===t._id} onClick={()=>status(t._id,t.status==='active'?'suspended':'active')}>{t.status==='active'?'Suspend':'Activate'}</button><button disabled={t.isSystem||busy===t._id} className="danger" onClick={()=>remove(t)}>Delete</button></td></tr>)}</tbody></table></div>}</main>}
export default function App(){return <BrowserRouter><Routes><Route path="/" element={<Dashboard/>}/><Route path="/construction" element={<ConstructionCore/>}/><Route path="/finance" element={<FinanceProcurement/>}/><Route path="/superadmin/companies" element={<Companies/>}/></Routes></BrowserRouter>}
